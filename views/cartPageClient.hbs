<div class="container d-flex flex-column">
<h3 class="h5 w-100 fw-bold mb-0 text-gray-800 text-start mt-5 mb-5">Số dư của bạn: {{Balance}} VNĐ
        </h3>
        {{#if cart.length}}
        <div>
            <table class="table">
                <thead>
                    <tr class="text-center">

                        <th scope="col" >Sách</th>
                        <th scope="col">Tác giả</th>
                        <th scope="col">Giá/ Sản phẩm</th>
                        <th scope="col">Số lượng</th>
                        <th scope="col">Đơn giá</th>
                        <th scope="col">Xóa</th>
                    </tr>
                </thead>
                <tbody id="listBook">
                    {{#each cart}}
                    <tr class="text-center">
                        <th scope="row">{{this.ProductName}}</th>
                        <td>{{this.Author}}</td>
                        <td>{{this.Price}} VNĐ</td>
                        <td>{{this.Quantity}}</td>
                        <td>{{this.TotalPrice}} VNĐ</td>
                        <td>
                            <a href="cartBook/delete?ID={{this.CartID}}" class="btn btn-danger" data-cart-id="{{this.CartID}}" >
                                <i class="fas fa-trash"></i>
                            </a>
                        </td>
                    </tr>
                    {{/each}}


                </tbody>
            </table>
        </div>
        <h3 class="h5 w-100 fw-bold mb-0 text-gray-800 text-center mt-5 mb-5">Tổng hóa đơn: {{TotalPriceAllItem}} VNĐ
        </h3>
        <a href="/cartBook/pay" class="align-self-center d-block btn btn-success"><i class="fa-solid fa-credit-card"></i> Xác nhận thanh toán
        </a>
        {{else}}
        <p class="text-center">Bạn chưa có sản phẩm nào trong giỏ hàng🥲</p>
        <h3 class="h5 w-100 fw-bold mb-0 text-gray-800 text-center mt-5 mb-5">Tổng hóa đơn: {{TotalPriceAllItem}} VNĐ
        </h3>
        {{/if}}
        
        </h3>



</div>

<script>
    function DeleteCartItem()
    {

    }




</script>